import { $ as VAULT_VERSION, A as zeroizeBuffer, B as MASTER_KEY_BYTES, C as generatePasswordSalt, D as parsePasswordKeyFile, E as parseMasterKey, F as ARGON2_PARALLELISM, G as SCRYPT_MAXMEM, H as PASSWORD_SALT_BYTES, I as ARGON2_TIME, J as SCRYPT_R, K as SCRYPT_N, L as ENCRYPTED_STORE_FORMAT, M as ARGON2_MAX_PARALLELISM, N as ARGON2_MAX_TIME, O as sha3_256Hex, P as ARGON2_MEMORY_KIB, Q as VAULT_KDF_INPUT, R as KEY_FILE_VERIFIER_REF, S as generateMasterKey, T as isPasswordKeyFile, U as REMEMBER_DAY_MS, V as NONCE_BYTES, W as REMEMBER_KEY_REF, X as VAULT_ALGORITHM, Y as TAG_BYTES, Z as VAULT_KDF, _ as deriveArgon2idKey, a as recoverKeyFromRemember, b as encodeMasterKey, c as parseDocument, d as serializeEncryptedStore, et as VaultError, f as verifyDocument, g as decryptEntryBuffer, h as decryptEntry, i as createRememberBlock, j as ARGON2_MAX_MEMORY_KIB, k as verifyEntryRecord, l as parseEncryptedStore, m as createPasswordKeyFile, n as unlockEncryptedStore, o as rememberActive, p as argon2Defaults, q as SCRYPT_P, r as verifyPasswordDigest, s as detectCredentialStore, t as encryptCredentialStore, u as serializeDocument, v as deriveMasterKey, w as isDigest, x as encryptEntry, y as deriveScryptKey, z as LEGACY_KDF } from "./vault-By6rgT8b.js";
export { ARGON2_MAX_MEMORY_KIB, ARGON2_MAX_PARALLELISM, ARGON2_MAX_TIME, ARGON2_MEMORY_KIB, ARGON2_PARALLELISM, ARGON2_TIME, ENCRYPTED_STORE_FORMAT, KEY_FILE_VERIFIER_REF, LEGACY_KDF, MASTER_KEY_BYTES, NONCE_BYTES, PASSWORD_SALT_BYTES, REMEMBER_DAY_MS, REMEMBER_KEY_REF, SCRYPT_MAXMEM, SCRYPT_N, SCRYPT_P, SCRYPT_R, TAG_BYTES, VAULT_ALGORITHM, VAULT_KDF, VAULT_KDF_INPUT, VAULT_VERSION, VaultError, argon2Defaults, createPasswordKeyFile, createRememberBlock, decryptEntry, decryptEntryBuffer, deriveArgon2idKey, deriveMasterKey, deriveScryptKey, detectCredentialStore, encodeMasterKey, encryptCredentialStore, encryptEntry, generateMasterKey, generatePasswordSalt, isDigest, isPasswordKeyFile, parseDocument, parseEncryptedStore, parseMasterKey, parsePasswordKeyFile, recoverKeyFromRemember, rememberActive, serializeDocument, serializeEncryptedStore, sha3_256Hex, unlockEncryptedStore, verifyDocument, verifyEntryRecord, verifyPasswordDigest, zeroizeBuffer };
