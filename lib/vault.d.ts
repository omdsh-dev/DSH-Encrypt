import { $ as SCRYPT_N, A as verifyEntryRecord, B as EntryRecord, C as generateMasterKey, D as parseMasterKey, E as isPasswordKeyFile, F as ARGON2_MEMORY_KIB, G as MASTER_KEY_BYTES, H as KdfName, I as ARGON2_PARALLELISM, J as ParsedEncryptedStore, K as NONCE_BYTES, L as ARGON2_TIME, M as ARGON2_MAX_MEMORY_KIB, N as ARGON2_MAX_PARALLELISM, O as parsePasswordKeyFile, P as ARGON2_MAX_TIME, Q as SCRYPT_MAXMEM, R as Argon2Params, S as encryptEntry, T as isDigest, U as KdfParams, V as KEY_FILE_VERIFIER_REF, W as LEGACY_KDF, X as REMEMBER_KEY_REF, Y as REMEMBER_DAY_MS, Z as RememberBlock, _ as decryptEntryBuffer, a as encryptCredentialStore, at as VAULT_KDF, b as deriveScryptKey, c as detectCredentialStore, ct as VaultError, d as serializeDocument, et as SCRYPT_P, f as serializeEncryptedStore, g as decryptEntry, h as createPasswordKeyFile, i as EncryptedStoreCreation, it as VAULT_ALGORITHM, j as zeroizeBuffer, k as sha3_256Hex, l as parseDocument, m as argon2Defaults, n as recoverKeyFromRemember, nt as ScryptParams, o as unlockEncryptedStore, ot as VAULT_KDF_INPUT, p as verifyDocument, q as PASSWORD_SALT_BYTES, r as rememberActive, rt as TAG_BYTES, s as verifyPasswordDigest, st as VAULT_VERSION, t as createRememberBlock, tt as SCRYPT_R, u as parseEncryptedStore, v as deriveArgon2idKey, w as generatePasswordSalt, x as encodeMasterKey, y as deriveMasterKey, z as ENCRYPTED_STORE_FORMAT } from "./vault-BwiX-hzp.js";
export { ARGON2_MAX_MEMORY_KIB, ARGON2_MAX_PARALLELISM, ARGON2_MAX_TIME, ARGON2_MEMORY_KIB, ARGON2_PARALLELISM, ARGON2_TIME, type Argon2Params, ENCRYPTED_STORE_FORMAT, type EncryptedStoreCreation, type EntryRecord, KEY_FILE_VERIFIER_REF, type KdfName, type KdfParams, LEGACY_KDF, MASTER_KEY_BYTES, NONCE_BYTES, PASSWORD_SALT_BYTES, type ParsedEncryptedStore, REMEMBER_DAY_MS, REMEMBER_KEY_REF, type RememberBlock, SCRYPT_MAXMEM, SCRYPT_N, SCRYPT_P, SCRYPT_R, type ScryptParams, TAG_BYTES, VAULT_ALGORITHM, VAULT_KDF, VAULT_KDF_INPUT, VAULT_VERSION, VaultError, argon2Defaults, createPasswordKeyFile, createRememberBlock, decryptEntry, decryptEntryBuffer, deriveArgon2idKey, deriveMasterKey, deriveScryptKey, detectCredentialStore, encodeMasterKey, encryptCredentialStore, encryptEntry, generateMasterKey, generatePasswordSalt, isDigest, isPasswordKeyFile, parseDocument, parseEncryptedStore, parseMasterKey, parsePasswordKeyFile, recoverKeyFromRemember, rememberActive, serializeDocument, serializeEncryptedStore, sha3_256Hex, unlockEncryptedStore, verifyDocument, verifyEntryRecord, verifyPasswordDigest, zeroizeBuffer };